apiVersion: v1
kind: Service
metadata:
  name: {{ include "fco.fullname" . }}-svc
  labels:
    {{- include "fco.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fco.selectorLabels" . | nindent 4 }}
